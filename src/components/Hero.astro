---

---

<section
  class="relative min-h-[85vh] md:min-h-screen flex items-center justify-center pt-20"
  aria-label="Hero section"
>
  <!-- Background Effects -->
  <div
    class="absolute inset-0 bg-gradient-to-br from-background via-muted/20 to-background"
    aria-hidden="true"
  >
  </div>
  <div class="absolute inset-0 bg-grid-pattern opacity-30" aria-hidden="true">
  </div>

  <!-- Content -->
  <div class="relative z-10 container mx-auto px-4 sm:px-6 text-center">
    <div class="max-w-4xl lg:max-w-5xl mx-auto space-y-4 md:space-y-8">
      <!-- Main Headlines -->
      <div class="space-y-4 md:space-y-6">
        <h1
          class="group headline text-2xl sm:text-3xl md:text-5xl lg:text-6xl xl:text-7xl text-foreground leading-[1.3] md:leading-[1.2]"
        >
          <span class="block">Manuelle Akquise nervt.</span>
          <span
            class="block text-lime-400 pb-1 relative after:content-[''] after:absolute after:bottom-[-10px] after:left-0 after:w-full after:h-0.5 after:bg-lime-400 after:scale-x-0 after:origin-right after:transition-transform after:duration-1000 after:ease-out group-hover:after:scale-x-100 group-hover:after:origin-left"
          >
            Lassen Sie das Ihre KI erledigen.
          </span>
        </h1>

        <!-- Subtitle -->
        <p
          class="body-text text-lg md:text-xl text-gray-400 max-w-2xl md:max-w-3xl mx-auto leading-relaxed"
        >
          Mehr Termine, weniger Stress. Wir bauen Ihre eigene Infrastruktur für
          die automatisierte Neukundengewinnung. 24/7.
        </p>
      </div>

      <!-- CTA Button -->
      <div class="pt-4 md:pt-8">
        <button
          onclick="window.openQuickAnalysis()"
          aria-label="Jetzt kostenlos starten"
          class="bg-primary text-primary-foreground hover:bg-primary/90 px-8 md:px-12 py-4 md:py-6 text-base md:text-lg font-semibold rounded-xl transition-colors duration-200 min-h-[48px] min-w-[48px]"
        >
          Jetzt kostenlos starten
        </button>
      </div>

      <!-- Trust Indicators -->
      <div class="pt-8 flex flex-col items-center justify-center gap-2">
        <p class="text-sm font-medium text-gray-500 uppercase tracking-wider">
          Bereits im Einsatz bei
        </p>
        <div
          class="flex flex-wrap justify-center gap-6 text-gray-400 font-semibold"
        >
          <span>Car Studio AI</span>
          <span class="text-gray-700">•</span>
          <span>ERPA</span>
          <span class="text-gray-700">•</span>
          <span>CemKimsan</span>
          <span class="text-gray-700">•</span>
          <span>Coach Bilge</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .bg-gradient-radial {
    background-image: radial-gradient(var(--tw-gradient-stops));
  }
</style>
